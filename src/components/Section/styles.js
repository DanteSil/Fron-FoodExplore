import styled from "styled-components";

export const Container = styled.section`
  padding-inline: 3.5rem;
  position: relative;

  margin-bottom: 1rem;

  max-width: 100vw;

  h2{
    font-family: 'Poppins';
    font-style: normal;
    font-weight: 500;
    font-size: 2.9rem;
    line-height: 140%;

    margin-bottom: 4rem;
  }
`